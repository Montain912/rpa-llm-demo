# 关闭浏览器起点的降耗对比

## 测试口径

- 基线：保存的 `stable-20260911`（`e12243c`），从独立目录启动，不覆盖当前项目。
- 优化版：上一轮 `c2e904f` 基础上进一步精简查询决策上下文。
- 同一远程桌面、同一 Edge 配置、同一账号、同一完整命令；不更换模型、不降低截图清晰度。
- 每轮开始前确认浏览器窗口关闭、桌面可见。前一轮成功退出后才关闭测试浏览器；失败轮单独保留，不能当作低成本成功。
- 这里的“冷启动”指浏览器窗口关闭后的任务起点，不是清除浏览器缓存、强杀全部后台进程或重启操作系统。
- 计入启动浏览器、网址输入和验收、登录、两项筛选、一次搜索、退出登录全过程的模型调用及 Token。关闭上一轮浏览器是两组一致的测试准备，不属于用户命令。
- 耗时采用各步骤 `duration.total` 之和，不包括启动服务和人工检查截图的时间。
- 完整通过必须保留两项选中状态，搜索执行一次、结果独立验收通过，并退出回登录页。保留所有失败记录；成功运行与失败运行分别统计。

## 本轮代码变化

1. 只读查询使用精简的决策说明，删除不属于当前能力范围的动态创建示例和重复说明，保留动作参数、登录阶段、输入纠错、范围边界与验收要求。原说明约 7,687 字符，精简后约 2,260 字符。
2. 查询历史保留最近五步的动作、参数、是否执行、输入状态、失败反馈、独立验收结果和坐标修正依据；只保留最新一步的完整 thought。完整运行日志不变。
3. 普通非查询任务继续使用原系统提示；独立文字识读和完成验收不压缩，输入执行不改动。复测另修复落点验收的一处质量问题：局部模型声称命中时不得丢弃几何边界，边界冲突或缺失不放行，避免误点相邻项。最新局部定位改为未标记截图的独立像素边界识读，由代码换算回原图并判断命中；这一最新改动尚未完成真实服务复测。
4. 同一阶段点击执行后，仅当目标标签与预期结果明确对应，或属于本阶段搜索时，直接请求原有独立验收；父菜单等中间控件不触发自动完成请求。登录提交后直接请求原有登录结果检查，省去一次通用决策。请求验收不等于通过；原有搜索次数、搜索后截图、登录入口和页面状态等门槛仍执行。
5. 实测发现验收器有时不把结果总数 0 识别为空结果，因此明确查询预期：与结果区域对应的总数明确为 0，且没有加载中或错误提示，也属于空结果；只有空白不算通过。未增加搜索次数，未跳过筛选保留和搜索后截图检查。
6. 设置环境变量 `RPA_LEAN_QUERY=0` 并重启服务，可以关闭本轮精简说明、精简历史与直接阶段验收调度，恢复上一轮对应行为（空结果预期描述仍保留）。此前优化开关仍然有效。
7. 导航验收条件收紧：主内容区必须已打开目标标签/页面，并显示筛选控件和结果区域。只在侧边栏露出菜单文字、主区域仍是首页仪表盘时，不算已进入目标页。

## 稳定版基线

| 日志时间（2026-09-11） | 步数 | 调用数 | 总 Token | 步骤耗时（秒） | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 12:25:41 | 27 | 54 | 286,257 | 110.76 | 完整通过，搜索一次 |
| 12:29:09 | 34 | 69 | 370,995 | 140.29 | 完整通过，搜索一次 |
| 12:32:51 | 15 | 23 | 136,036 | 56.06 | 登录流程未完成，自动填充项点击被策略拦截；未搜索 |

三次尝试中两次完整通过。完整通过的两轮平均为 328,626 Token、125.53 秒；失败的 136,036 Token 单列，不用这条不完整流程拉低完整任务平均消耗。全部三次尝试实际消耗为 793,288 Token。

独立基线目录：`D:\自动操作agent\benchmarks\cold-start-20260911\baseline`。窗口关闭截图、只用于准备的 `desktop.py` 和只读统计脚本 `report.py` 位于其上一级目录。两组日志目录不同，原版会在历史上下文中携带本地路径，因此存在小量路径文本差异。

## 结果解释

“减半”的目标相对于保存的稳定版，而不是相对于单次偶然高消耗运行。模型决策、网络耗时仍有波动；少量同起点测试可以证明本批运行表现，不能证明所有任务、所有页面都达到固定比例或相同成功率。

## 优化迭代中的非通过样本

- `logs/task_20260911_123552.jsonl`，运行编号 `603603`：浏览器关闭起点，搜索已执行一次，但空结果验收失败后提出重复搜索，被拦截，最终失败。共 31 步、60 次调用、177,363 Token；未计作成功。失败后单独清理退出登录，不补算为该任务成功。
- 运行编号 `208567`（`12:43:36`）：准备时关闭一个浏览器后仍残留第二个窗口；误启动后发现起点不合格，发送终止请求，7 步终止，9 次调用、28,888 Token、27.04 秒。保留原始日志和消耗，排除出冷启动对照样本，不用它宣称降耗。
- 运行编号 `174361`（`12:49:11`）：浏览器关闭起点。独立验收把侧边栏展开误判为已进入目标页，后续真正导航被阶段限制拦截；15 步、24 次调用、71,794 Token、60.33 秒，未搜索，失败。随后收紧导航验收条件，并限制自动验收只对明确结果目标发起。

正式复测前再次截图确认桌面，不仅检查“关闭命令已执行”。

- `12:54:43`，运行编号 `593122`：28 步、43 次调用、136,759 Token、99.32 秒，未搜索。声明点击智能问数，实际误选相邻行；局部语义放行丢弃了边界，随后重复同坐标被循环保护阻止。修复为局部语义与几何同时满足，不关闭循环保护。
- `13:02:56`，运行编号 `411371`：网址验收后模型服务报告 Connection error，未进入业务查询；作为服务异常样本保留，不计成功。
- `13:05:14`，运行编号 `341548`：27 步、49 次调用、139,040 Token、114.84 秒，未搜索。严格几何检查挡住了错误坐标，但局部定位又混用图片尺寸，连续三次无法可靠定位而失败。随后改为独立像素边界定位，待实测。

## 中间修正版复测

| 日志时间（2026-09-11） | 运行编号 | 步数 | 调用数 | 总 Token | 步骤耗时（秒） | 结果 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 12:45:41 | 306456 | 32 | 53 | 133,833 | 121.52 | 完整通过，搜索一次 |

第一轮完整复测相对基线完整成功均值的 Token 降幅为 59.3%；此处先记录单轮结果，后续仍需不改代码重复验证。起点证据为 `desktop-1789101913548235400-closed.png`，最终退出截图为 `screenshots/sh_306456_32.png`。

收紧导航验收前，158 项回归测试通过，包含新增的“直接阶段验收不能越过独立失败结果”和“直接请求规划不能跳过登录入口证据”，并在服务使用的 Python 环境验证。随后新增父菜单不得触发自动完成请求的测试，总计 159 项。

## 当前检查点：未达到稳定减半结论

- 最新本地回归测试 162 项通过，包括语义不得覆盖边界冲突、缺少边界不得通过、未标记局部图像素边界换算、越界坐标拒绝。均使用模拟模型，不外发截图。
- 最新定位方案的真实截图重放被安全审批阻止：需要用户明确授权把本任务内部页面截图发送到项目现有 DeepSeek 接口。没有绕过该审批，也没有把未执行的重放计为成功。
- 本轮优化迭代已记录的模型消耗合计 710,424 Token，包含失败和不合格起点运行；基线尝试合计 793,288 Token。此合计是实验成本，不是单次任务降耗比例。连接错误轮统计为 8 次已记录调用、22,747 Token；异常调用若服务端已计费但未返回 usage，无法从本地日志核算。
- 中间版本只有一轮完整成功，不能据此声称稳定成功率或最终版消耗已减半。完整成功单轮 133,833 Token 对基线成功均值 328,626 的降幅为 59.3%，仅作为潜力证据。
- 已停止测试服务。失败后的退出与关窗仅为清理，不补算任务成功。原 `stable-20260911` 保留不变，当前改动作为待复测检查点保存。
