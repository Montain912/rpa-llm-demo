# 性能优化与验证

## 保存的基线

优化前稳定版：Git 标签 `stable-20260911`，提交 `e12243c`。外部文件快照仍保留在 `D:\自动操作agent\versions\rpa-llm-demo-fresh-stable-20260911`。本轮不移动该标签。

## 已实施的优化

| 方面 | 实现 | 质量保障 |
| --- | --- | --- |
| 历史上下文 | 保留最近五步的语义信息，去掉截图路径、耗时、像素评分等诊断数据，使用紧凑 JSON | 完整日志不变；保留执行状态、原因、独立证据、修正坐标和子任务信息 |
| 分阶段知识 | 索引用 `promptPhases` 声明知识适用阶段；业务操作阶段不重复携带浏览器启动、输入法详细知识 | 启动、待验收输入、失败恢复时使用完整知识；未声明阶段的业务知识始终保留；系统基本规则和范围策略不变 |
| 网址验收调度 | 受控网址输入后直接调用原有独立文字识读器，不先让通用模型输出验收动作 | 识读器不接收目标网址或历史，仍核对实际字符、可读性和输入法候选条；失败仍进入原有纠错流程 |
| 落点修正调度 | 独立验收器给出有效建议后，下一步复用同一目标的建议坐标一次 | 不直接点击；仍执行新截图、新鲜度检查、循环检查、独立落点验证和范围检查；禁止无限自动重试 |
| 登录状态 | 明确提示本次用户名、密码、提交所处阶段；首次启动独立确认前台浏览器后导航指定入口 | 不复用上一轮登录证据，不将预填内容当成本次验收成功；桌面图标不算前台浏览器证据 |
| 输入框与下拉框 | 输入框修正时局部放大核对文字行，避免两行间留白误判；消除下拉框一律使用键盘的冲突提示 | 保留密码掩码验收和勾选/标签验收，不放宽业务操作白名单 |
| 消耗统计 | 每次成功模型调用记录调用用途、API 耗时、文字字符数，以及视觉调用图片尺寸和格式 | 不更换模型，不降低截图清晰度，不减少最终结果验收 |

## 优化开关

以下环境变量默认开启；设为 `0` 并重启服务可单独关闭，用于对照测试：

- `RPA_COMPACT_HISTORY`：历史上下文精简。
- `RPA_PHASE_KNOWLEDGE`：业务阶段知识精简。
- `RPA_DIRECT_URL_VERIFY`：网址验收直接调度。
- `RPA_POINTER_RELOCATION`：独立验收器建议坐标的直接调度。

这些开关不是整版回退；恢复完整稳定版应使用已保存的版本。

## 验证口径

完整任务：从指定 `login.html` 登录，进入体验中心的智能体能力页面，选择智能问数与新SQL生成，搜索一次，退出登录。不得测试其他业务功能。

完成须同时满足：本次登录证据、两项已选状态、一次实际搜索及结果验收、退出后的登录页验收。测试失败也保留日志，不能以只跑部分流程的低消耗作为优化成绩。

历史三份成功运行记录的上下文离线对比显示：历史部分字符数减少约 51%～53%；这是字符数，不是总 Token 或费用的同等降幅。当前任务业务阶段知识从 3,870 字符减至 2,398 字符。

历史不同运行的页面起点、动作数和模型响应存在差异，实测总耗时/Token 对比属于观察结果，不是严格配对实验或稳定成功率保证。

## 暂未实施

本轮未降低图像分辨率、压缩输入验收、缩短底层按键间隔、取消独立落点检查或更换模型。自适应等待、图像保存去重留待后续独立验证。

## 迭代实测记录（2026-09-11）

耗时为日志各步骤 `duration.total` 之和，不含服务启动；Token 为服务返回的用量统计，包含决策和独立验收。

| 记录 | 步数 | 模型调用 | 总 Token | 步骤耗时（秒） | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 历史 `100501` | 32 | 59 | 337,168 | 148.05 | 完整通过 |
| 历史 `102134` | 22 | 47 | 244,101 | 112.63 | 完整通过 |
| 历史 `111236` | 31 | 60 | 329,227 | 164.26 | 完整通过 |
| 迭代 `113001` | 10 | 14 | 81,975 | 27.95 | 未取得本次分阶段登录证据，失败 |
| 迭代 `113317` | 14 | 20 | 116,113 | 67.58 | 密码输入焦点错误，失败 |
| 迭代 `113743` | 20 | 44 | 202,868 | 98.35 | 收起弹层的含糊点击被拦截，失败 |
| 迭代 `114120` | 4 | 9 | 37,396 | 17.26 | 复用残留业务页状态后尝试含糊点击，失败 |
| 迭代 `114336` | 32 | 59 | 267,220 | 142.14 | 完整通过，之后继续优化识读提示和落点修正调度 |
| 最终版本 `114727` | 30 | 52 | 228,702 | 130.46 | 完整通过 |
| 最终版本 `115012` | 24 | 46 | 194,274 | 116.85 | 不改代码复测，完整通过 |

日志均位于 `logs/task_20260911_对应记录.jsonl`，用量位于 `summary/token_usage_*.json`。失败运行的消耗不能当作完整任务的降耗效果，也不能从测试总成本中忽略。

最终版本回归测试：153 项通过，已同时在系统 Python 和服务所用 Python 3.10 环境验证。新增测试覆盖上下文不修改原日志、阶段知识回退、网址实际文字不匹配不得通过、未确认浏览器不得自动导航、修正坐标仍须独立落点验收等。

最终同一代码版本连续两次完整通过，各只执行一次搜索，五个子任务均有独立截图验收，最后返回登录页。两次用量文件为 `summary/token_usage_95171.json` 与 `summary/token_usage_108790.json`。末轮搜索结果截图 `screenshots/sh_108790_22.png` 显示两项筛选标签保留、模型 0 个；退出截图为 `screenshots/sh_108790_24.png`。

最终两轮平均总 Token 为 211,488，步骤耗时 123.66 秒；历史三轮成功记录平均为 303,498.67 Token、141.65 秒。观察到的平均降幅分别约为 30.3% 与 12.7%。样本较少且非相同起点的配对实验，不能据此承诺固定提速或总体成功率。
