# RPA + LLM 项目优化对比

## 主要变化

| 方面 | 最初实现 | 当前实现 |
| --- | --- | --- |
| 键盘输入 | 主要依赖逐键发送和模型判断，缺少完整的字段状态控制 | 独立输入法模块统一处理语言切换、字段规范化和特殊字符映射；增加输入前准备、输入后验收及按原因纠错，避免重复追加和未验收提交 |
| 鼠标操作 | 主要依赖模型坐标执行移动、点击和滚动，参数及异常处理较简单 | 统一校验坐标和动作参数，支持分段移动、可控滚动与拖拽、异常释放；执行前检查坐标新鲜度和目标落点，减少误操作 |
| 视觉细节处理 | 主要使用全屏截图，细小文字和相邻控件容易混淆 | 结合全屏上下文、局部裁剪放大和 PNG 无损传输，提高细节核对能力 |
| 操作状态判断 | 容易将画面变化或模型描述当作操作成功 | 区分动作已发送、画面已变化和目标已完成，依据字段值、标签、勾选或其他状态证据验收 |
| 循环检测 | 较依赖相邻动作比较，难以识别来回重复 | 使用规范化动作签名和坐标量化识别短周期循环，并结合画面变化判断是否推进 |
| 循环恢复 | 重复动作容易持续执行或直接停止 | 循环后重新观察并规划当前未完成步骤，保留已完成状态；同一步两次重新规划仍无进展才停止 |
| 完成判断 | 较依赖模型口头宣告完成 | 子任务增加独立截图验收，整体完成与各阶段验收结果绑定 |



